#include "grains.h"

uint64_t square(uint8_t index) {
	uint64_t grains = (uint64_t) pow(2, index - 1);
	return grains;
}

uint64_t total(void) {
	uint64_t total = (uint64_t) pow(2, 64) - 1;
	return total;
}
